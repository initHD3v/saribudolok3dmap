{
  "name": "saribudolok-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix apps/server\" \"npm run dev --prefix apps/web\"",
    "install:all": "npm install --prefix apps/server && npm install --prefix apps/web",
    "seed": "npm run seed --prefix apps/server"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.2.1"
  }
}
